me = "adhitya"
print(len(me)) # len is used to know the length of the string

print(me.endswith("tya"))  # endswith is used to know weather the sring ends with the certain letters

count = me.count("a")    # counts the total number of occurrences of any character
print(count)

replaced_string = me.replace("h", "") # This function replace the old word with new word in the entire string.
print(replaced_string)

